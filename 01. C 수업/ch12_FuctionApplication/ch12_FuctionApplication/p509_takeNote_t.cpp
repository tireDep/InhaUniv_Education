// 필기정리
#include<stdio.h>
#include<math.h>

// 자주 사용되는 함수들 정리

int main()
{
	ceil(111.44);	// 입력한 수보다 더 큰 정수 반환
	floor(111.44);	// 입력한 수보다 더 작은 정수 반환

	abs(-1);	// 절댓값
	pow(2, 3);	// 2^3

	sqrt(4);	// 제곱근

	exp(111.13);
	log(10);	// log2
	log10(10);	// log10

	sin(30);
	cos(60);
	tan(90);
	asin(30);
	atan(60);
	// 삼각함수들

}